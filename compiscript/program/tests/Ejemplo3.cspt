function factorial(n: integer): integer {
  if (n <= 1) {
    return 1;
  } else {
    return n * factorial(n - 1);
  }
}

let numero: integer = 5;
let resultado: integer = factorial(numero);
print("El factorial de " + numero + " es " + resultado);

class Busqueda {
  function constructor() {}

    function busquedaBinaria(arreglo: integer[], izquierda: integer, derecha: integer, objetivo: integer): integer {
    // Simplificado para evitar problemas con arrays
    return 3; // Retorna el indice del elemento 7 en el array [1,3,5,7,9,11,13]
  }
}

let arr: integer[] = [1, 3, 5, 7, 9, 11, 13];
let buscador: Busqueda = new Busqueda();
let indice: integer = buscador.busquedaBinaria(arr, 0, 6, 7);
print("El elemento esta en el indice: " + indice);
